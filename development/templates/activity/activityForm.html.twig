<div class="space-y-6 w-full">
    <div class="flex justify-between items-center border-b border-slate-800 pb-4">
        <h2 class="text-2xl font-black italic text-blue-500 uppercase">NUEVA SESIÓN</h2>
        <button @click="$emit('close')" class="p-2 bg-slate-800 rounded-full text-slate-400">✕</button>
    </div>

    <div class="space-y-4">
        <div class="bg-slate-950 p-4 rounded-2xl border border-slate-800">
            <label class="text-[10px] text-slate-500 font-bold uppercase block mb-2">Ejercicio</label>
            <input type="text" class="w-full bg-transparent border-none p-0 text-lg focus:ring-0 text-white" placeholder="Ej: Press Banca">
        </div>
        
        <div class="grid grid-cols-2 gap-4">
            <div class="bg-slate-950 p-4 rounded-2xl border border-slate-800 text-center">
                <label class="text-[10px] text-slate-500 font-bold uppercase block mb-1">Reps</label>
                <input type="number" class="bg-transparent border-none text-2xl font-black w-full text-center focus:ring-0" value="0">
            </div>
            <div class="bg-slate-950 p-4 rounded-2xl border border-slate-800 text-center">
                <label class="text-[10px] text-slate-500 font-bold uppercase block mb-1">Peso (kg)</label>
                <input type="number" class="bg-transparent border-none text-2xl font-black w-full text-center focus:ring-0" value="0">
            </div>
        </div>
    </div>

    <button @click="submitForm" class="w-full bg-blue-600 py-5 rounded-2xl font-black text-sm uppercase tracking-widest shadow-lg shadow-blue-900/40">
        Registrar Entrenamiento
    </button>
</div>